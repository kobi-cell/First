# WORKIES AIO — טבלת Popups (Mockup v2)

טבלה מרוכזת של 8 המסכים הקופצים (Popups) מהמוקאפ המלא, לשימוש מוצר/פיתוח/QA.

---

| Popup ID | שם Popup | מסך אם | טריגר (כפתור) | CTA ראשי | שדות חובה | ולידציות קריטיות | הרשאות ביצוע | אינטגרציות/אוטומציות |
|---|---|---|---|---|---|---|---|---|
| POP-01 | שליחת תזכורת תשלום | גבייה (Collections) | "שלח תזכורת" | "שלח תזכורת" | לקוח, חוב פתוח, סוג תזכורת, תבנית הודעה | חייב להיבחר ערוץ שליחה; תבנית לא ריקה | כספים/גבייה `E`, הנהלה `V`, Admin `M` | Notification service פנימי + Audit + CommunicationLog |
| POP-02 | חשבונית חדשה | גבייה (Collections) | "+ חשבונית חדשה" | "הפק חשבונית" | לקוח, סכום, מועד תשלום, תיאור | סכום גדול מ-0; תאריך תקין | כספים/גבייה `E`, הנהלה `V`, Admin `M` | Sumit create document + sync חשבונית ל-Workies |
| POP-03 | עדכון הסדר תשלום | גבייה (Collections) | "עדכן הסדר" | "שמור הסדר" | לקוח, חוב כולל, מספר תשלומים, תשלום ראשון | מספר תשלומים >= 2; תאריך ראשון תקין | כספים/גבייה `E/A` לפי סף, הנהלה `V`, Admin `M` | יצירת PaymentPlan + יצירת משימות Follow-up |
| POP-04 | שליחת הצעת מחיר לחתימה דיגיטלית | Pipeline | "שלח הצעת מחיר" | "שלח להצעה וחתימה" | לקוח, שלב Pipeline, ערך חוזה, משרד מוצע, תאריך תחילה, ספק חתימה, מייל לקוח | מייל תקין; אין שליחה ללא ערך חוזה ותאריך | מכירות `E`, שיווק `E`, הנהלה `V`, Admin `M` | DocuSign (או ספק חתימה) + עדכון סטטוס ליד |
| POP-05 | עדכון מצב חידוש חוזה | חוזים וחידושים | "עדכן חידוש"/"עדכן מו\"מ" | "שמור עדכון" / "שלח הצעה" | סטטוס מו"מ, תנאים מוצעים, פגישה/שיחה הבאה | תאריך פגישה חובה בסטטוס מו"מ/ממתין לחתימה | מכירות `E`, תפעול `E`, הנהלה `V/A`, Admin `M` | עדכון סטטוס Renewal + יצירת התראת מעקב |
| POP-06 | עדכון הסכם שנחתם | חוזים וחידושים | "עדכן הסכם שנחתם" | "עדכן ובצע פעולות נלוות" | תאריך חתימה, תוקף התחלה, משרד/שולחנות | לא ניתן לשמור בלי תאריכים ושיוך יחידה | מכירות `E`, תפעול `E`, הנהלה `A`, Admin `M` | עדכון Pipeline לסגירה + חשבונית ראשונה + Onboarding 30/60/90 + עדכון תפוסה + מייל ברוכים הבאים |
| POP-07 | עדכון סיום הסכם / עזיבה | חוזים וחידושים | "עזיבה"/"עדכן סיום הסכם" | "עדכן סיום הסכם" | סיבת עזיבה, תאריך פינוי, פיקדון להחזר | חובה תאריך פינוי; אם יש חוב פתוח - יצירת טיפול גבייה | תפעול `E`, כספים/גבייה `E/A` לסגירת חוב, הנהלה `A`, Admin `M` | סטטוס חוזה "מסתיים" + שחרור משרד + עצירת חיובים + משימת גבייה + משימת שיווק לאכלוס |
| POP-08 | אישור דוח P&L חודשי | P&L חודשי | "P&L אשר" | "P&L אשר" | תקופה, הכנסות, הוצאות, רווח נקי, מרווח | רווח נקי = הכנסות-הוצאות; מרווח מחושב אוטומטית | הנהלה `A`, כספים/גבייה `E` להכנה, Admin `M` | ApprovalEvent + נעילת דוח + סימון מוכנות לייצוא SAP |

---

## שדות סטטוס מומלצים ל-QA בכל Popup
- מצב פתיחה: `open`
- מצב שמירה מוצלח: `saved/sent/approved`
- מצב כשל: `failed`
- מצב חלקי (בפופאפים עם פעולות נלוות): `partial_success` (POP-06/07/08)

---

## מפתח הרשאות קצר
- `V` = צפייה
- `E` = ביצוע/עריכה
- `A` = אישור
- `M` = ניהול מלא
